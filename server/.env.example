# Clerk issuer domain used by Convex JWT provider.
CLERK_JWT_ISSUER_DOMAIN=https://your-domain.clerk.accounts.dev

# Optional Expo push endpoint override.
EXPO_PUSH_ENDPOINT=https://exp.host/--/api/v2/push/send

# Optional access token for Expo push service.
EXPO_ACCESS_TOKEN=
